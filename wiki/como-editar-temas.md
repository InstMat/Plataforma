# Cómo editar temas y estilos

1. Modifica los archivos `.scss` o `.css` en la carpeta `reveal/css/theme/` o `styles/`.
2. Compila los temas Sass si es necesario usando `npm run build -- css-themes`.
3. Prueba los cambios en las presentaciones y landing pages.
